import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tuempresa.schoolevents',
  appName: 'SchoolEvents',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
